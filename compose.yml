services:
  ytdlp:
    # build:
    #   context: .
    #   dockerfile: dockerfile
    image: zhangyiming748/ytdlp:v0.0.4
    container_name: ytdlp
    volumes:
      - ./:/data
    command: top
    environment:
      - PROXY=http://192.168.5.2:8889